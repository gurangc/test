#include <iostream>
#include "add.hpp"
#include "sub.hpp"
using namespace std;

int main(void)
{
  int sum, diff, a, b;
  cin >> a >> b;
  sum = add(a, b);
  diff = sub(a, b);
  cout << "sum: " << sum << endl;
  cout << "diff: " << diff << endl;
  
  return 0;
}
